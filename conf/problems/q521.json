{
  "id": "q521",
  "title": "找最小数",
  "score": 100,
  "tags": [
    "E卷"
  ],
  "sections": [
    {
      "type": "description",
      "title": "题目描述",
      "content": "最新华为OD机试\n\n给一个正整数NUM1，计算出新正整数NUM2，NUM2为NUM1中移除N位数字后的结果，需要使得NUM2的值最小。"
    },
    {
      "type": "input_spec",
      "title": "输入描述",
      "content": "1.输入的第一行为一个\n字符串\n，\n字符串\n由0-9字符组成，记录正整数NUM1，NUM1长度小于32。\n2.输入的第二行为需要移除的数字的个数，小于NUM1长度。"
    },
    {
      "type": "output_spec",
      "title": "输出描述",
      "content": "输出一个数字字符串，记录\n最小值\nNUM2。"
    },
    {
      "type": "examples",
      "title": "用例",
      "content": "输入"
    }
  ],
  "solutions": {
    "Python": "# 输入获取\nnum1 = input()\nremoveCount = int(input())\n\n\n# 算法入口\ndef getResult(num1, removeCount):\n    if len(num1) == removeCount:\n        return \"0\"\n\n    remainCount = len(num1) - removeCount\n\n    stack = []\n    for i in range(len(num1)):\n        while len(stack) > 0 and removeCount > 0 and stack[-1] > num1[i]:\n            stack.pop()\n            removeCount -= 1\n        stack.append(num1[i])\n\n    while len(stack) > remainCount:\n        stack.pop()\n\n    while stack[0] == '0' and len(stack) != 1:\n        stack.pop(0)\n\n    return \"\".join(stack)\n\n\n# 算法调用\nprint(getResult(num1, removeCount))"
  },
  "verified": false
}