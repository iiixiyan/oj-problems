{
  "id": "q548",
  "title": "英文输入法",
  "score": 100,
  "tags": [
    "E卷"
  ],
  "sections": [
    {
      "type": "description",
      "title": "题目描述",
      "content": "最新华为OD机试\n\n主管期望你来实现英文\n输入法\n单词联想功能。\n需求如下：\n依据用户输入的单词前缀，从已输入的英文语句中联想出用户想输入的单词，按字典序输出联想到的单词序列，\n如果联想不到，请输出用户输入的单词前缀。"
    },
    {
      "type": "note",
      "title": "注意",
      "content": "：\n英文单词联想时，区分大小写\n缩略形式如”don’t”，判定为两个单词，”don”和”t”\n输出的单词序列，不能有重复单词，且只能是英文单词，不能有标点符号"
    },
    {
      "type": "input_spec",
      "title": "输入描述",
      "content": "输入为两行。\n首行输入一段由英文单词word和标点符号组成的语句str；\n接下来一行为一个英文单词前缀pre。\n0 < word.length() <= 20\n0 < str.length <= 10000\n0 < pre <= 20"
    },
    {
      "type": "output_spec",
      "title": "输出描述",
      "content": "输出符合要求的单词序列或单词前缀，存在多个时，单词之间以单个空格分割"
    },
    {
      "type": "examples",
      "title": "示例1",
      "content": "输入"
    }
  ],
  "solutions": {
    "Python": "import re\n\n# 输入获取\ns = input()\npre = input()\n\n\n# 算法入口\ndef getResult(s, pre):\n    tmp = re.split(\"[^a-zA-Z]\", s)\n    cache = list(set(tmp))\n    cache.sort()\n    cache = list(filter(lambda x: x.startswith(pre), cache))\n    if len(cache) > 0:\n        return \" \".join(cache)\n    else:\n        return pre\n\n\n# 算法调用\nprint(getResult(s, pre))"
  },
  "verified": false
}