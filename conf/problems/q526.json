{
  "id": "q526",
  "title": "数组拼接",
  "score": 100,
  "tags": [
    "E卷",
    "数组"
  ],
  "sections": [
    {
      "type": "description",
      "title": "题目描述",
      "content": "最新华为OD机试\n\n现在有多组整数数组，需要将它们合并成一个新的数组。\n合并规则，从每个数组里按顺序取出固定长度的内容合并到新的数组中，取完的内容会删除掉，\n如果该行不足固定长度或者已经为空，则直接取出剩余部分的内容放到新的数组中，继续下一行。"
    },
    {
      "type": "input_spec",
      "title": "输入描述",
      "content": "第一行是每次读取的固定长度，0<长度<10\n第二行是整数数组的数目，0<数目<1000\n第3-n行是需要合并的数组，\n不同的\n数组用回车换行分隔，数组内部用逗号分隔，最大不超过100个元素。"
    },
    {
      "type": "output_spec",
      "title": "输出描述",
      "content": "输出一个新的数组，用逗号分隔。"
    },
    {
      "type": "examples",
      "title": "示例1",
      "content": "输入"
    }
  ],
  "solutions": {
    "Python": "# 输入获取\nsubLen = int(input())\nn = int(input())\n# 输入用例中的数组存在 4,,6 这种情况,所以这里需要过滤掉空串\nlists = [list(filter(lambda x: x != \"\", input().split(\",\"))) for _ in range(n)]\n\n\n# 算法入口\ndef getResult():\n    ans = []\n\n    while len(lists) > 0:\n        lst = lists.pop(0)\n        tmp = lst[:subLen]\n        del lst[:subLen]\n        ans.extend(tmp)\n        if len(lst) > 0:\n            lists.append(lst)\n\n    return \",\".join(ans)\n\n\n# 算法调用\nprint(getResult())"
  },
  "verified": false
}