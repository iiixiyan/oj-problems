{
  "id": "q555",
  "title": "预订酒店",
  "score": 100,
  "tags": [
    "E卷"
  ],
  "sections": [
    {
      "type": "description",
      "title": "题目描述",
      "content": "最新华为OD机试\n\n放暑假了，小明决定到某旅游景点游玩，他在网上搜索到了各种价位的酒店（长度为n的数组A），他的心理价位是x元，请帮他筛选出k个最接近x元的酒店（n>=k>0）,并\n由低到高\n打印酒店的价格。"
    },
    {
      "type": "input_spec",
      "title": "输入描述",
      "content": "第一行：n, k, x\n第二行：A[0] A[1] A[2]…A[n-1]"
    },
    {
      "type": "output_spec",
      "title": "输出描述",
      "content": "从低到高打印筛选出的酒店价格"
    },
    {
      "type": "examples",
      "title": "示例1",
      "content": "输入"
    }
  ],
  "solutions": {
    "Python": "# 输入获取\nn, k, x = map(int, input().split())\nprices = list(map(int, input().split()))\n\n\n# 算法入口\ndef getResult(n, k, x, prices):\n    tmp = []\n\n    for p in prices:\n        tmp.append((p, abs(p - x)))\n\n    tmp.sort(key=lambda x: (x[1], x[0]))\n\n    ans = list(map(lambda x: x[0], tmp[:k]))\n\n    ans.sort()\n\n    return \" \".join(map(str, ans))\n\n\n# 调用算法\nprint(getResult(n, k, x, prices))"
  },
  "verified": false
}