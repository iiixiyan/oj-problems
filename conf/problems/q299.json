{
  "id": "q299",
  "title": "字符串变换最小字符串",
  "score": 100,
  "tags": [
    "字符串"
  ],
  "sections": [
    {
      "type": "description",
      "title": "题目描述",
      "content": "给定一个字符串s，最多只能进行一次变换，返回变换后能得到的最小字符串（按照字典序进行比较）。\n变换规则：交换字符串中任意两个不同位置的字符。"
    },
    {
      "type": "input_spec",
      "title": "输入描述",
      "content": "一串小写字母组成的字符串s"
    },
    {
      "type": "output_spec",
      "title": "输出描述",
      "content": "按照要求进行变换得到的最小字符串。"
    },
    {
      "type": "note",
      "title": "备注",
      "content": "s是都是小写字符组成\n1 ≤ s.length ≤ 1000"
    },
    {
      "type": "examples",
      "title": "用例",
      "content": "输入\nabcdef\n输出\nabcdef",
      "cases": [
        {
          "input": "abcdef",
          "output": "abcdef",
          "explanation": ""
        }
      ]
    },
    {
      "type": "note",
      "title": "说明",
      "content": "abcdef已经是最小字符串，不需要交换。\n输入\nbcdefa\n输出\nacdefb\n\na和b进行位置交换，可以得到最小字符串"
    },
    {
      "type": "analysis",
      "title": "题目解析",
      "content": "我的思路如下，先将输入的字符串str进行字典排序，得到一个新串minStr，如果str和minStr相同，则"
    },
    {
      "type": "note",
      "title": "说明",
      "content": "str已经是最小字符串，不需要任何变换，直接返回str。\n如果str和minStr不同，则\n\nstr不是最小字符串，我们需要遍历出str的每一个字符，对比str[i] 和 minStr[i] ，具体处理逻辑如下图\nJava算法源码"
    }
  ],
  "solutions": {
    "Python": "# 输入获取\ns = input()\n\n\n# 算法入口\ndef getResult():\n    minSArr = list(s)\n    minSArr.sort()\n\n    if s == \"\".join(minSArr):\n        return s\n\n    sArr = list(s)\n\n    for i in range(len(s)):\n        if sArr[i] != minSArr[i]:\n            tmp = sArr[i]\n            sArr[i] = minSArr[i]\n\n            swapIdx = s.rindex(minSArr[i])\n            sArr[swapIdx] = tmp\n            break\n\n    return \"\".join(sArr)\n\n\n# 调用算法\nprint(getResult())"
  },
  "verified": false
}